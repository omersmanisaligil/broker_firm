databaseChangeLog:
  - changeSet:
      id: 1
      author: Ömer Seyfettin Manisalıgil
      changes:
        - createTable:
            tableName: asset
            columns:
              - column:
                  name: id
                  type: UUID
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: name
                  type: varchar(16)
                  constraints:
                    nullable: false
              - column:
                  name: size
                  type: numeric
                  constraints:
                    nullable: false
              - column:
                  name: usable_size
                  type: numeric
                  constraints:
                    nullable: false
              - column:
                  name: user_id
                  type: UUID
                  constraints:
                    foreignKeyName: fk_asset_user_id
                    references: users(id)
                    nullable: false
        - createIndex:
            tableName: asset
            indexName: idx_asset_user_id
            columns:
              column:
                name: user_id
        - addUniqueConstraint:
            tableName: asset
            columnNames: name, user_id